apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: backend
data:
  DB_HOST: "postgres-svc.database.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "healthtrack"
  DB_USER: "postgres"
  DB_PASSWORD: "postgres"
